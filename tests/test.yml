---
- hosts: 127.0.0.1
  connection: local
  remote_user: ubuntu
  gather_facts: false
  vars:
    dotenv:
      path: "/home/ubuntu/test"
      user: "ubuntu"
      group: "ubuntu"
      vars:
        rails_env: "production"
  roles:
    - ansible-dotenv
